<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ashwin.viewpagersandbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ViewPagerSandbox">
        <activity android:name=".fragtab1.FragTab1Activity"></activity>
        <activity android:name=".frag.FragActivity" />
        <activity android:name=".viewtab.ViewTabActivity" />
        <activity android:name=".next.NextActivity" />
        <activity android:name=".fragtab2.FragTab2Activity" />
        <activity android:name=".view.ViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>